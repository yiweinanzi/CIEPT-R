# Metrics Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Implement `T011` by adding reusable ranking, faithfulness, and usage-diagnosis metrics that align with the project’s evaluation formulas.

**Architecture:** Add a dedicated `ciept.metrics` package split by metric family. Keep the functions pure and JSON-friendly so later experiment runners can compose them without hidden side effects or protocol coupling.

**Tech Stack:** Python 3.10, dataclasses, math, pytest

---

## Chunk 1: Ranking And Faithfulness Metrics

### Task 1: Add failing tests for ranking and faithfulness formulas

**Files:**
- Create: `tests/test_metrics_ranking.py`
- Create: `tests/test_metrics_faithfulness.py`
- Reference: `docs/superpowers/specs/2026-03-10-metrics-design.md`

- [ ] **Step 1: Write the failing test**

```python
from ciept.metrics.ranking import recall_at_k, ndcg_at_k, mrr_at_k


def test_ranking_metrics_return_expected_toy_values():
    ...
```

```python
from ciept.metrics.faithfulness import sufficiency_gap, comprehensiveness_gap, support_precision_recall_f1


def test_faithfulness_metrics_follow_expected_directions():
    ...
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest tests/test_metrics_ranking.py tests/test_metrics_faithfulness.py -v`
Expected: FAIL because the metrics package does not exist yet.

- [ ] **Step 3: Write minimal implementation**

Create:

- `src/ciept/metrics/__init__.py`
- `src/ciept/metrics/types.py`
- `src/ciept/metrics/ranking.py`
- `src/ciept/metrics/faithfulness.py`

Implementation requirements:

- ranking metrics support toy positive-vs-negative comparisons
- faithfulness metrics follow the repository’s ratio-based leakage semantics
- metric dataclasses contain plain floats

- [ ] **Step 4: Run test to verify it passes**

Run: `python -m pytest tests/test_metrics_ranking.py tests/test_metrics_faithfulness.py -v`
Expected: PASS

- [ ] **Step 5: Leave changes uncommitted for the single-task final commit**

Record progress locally, but do not create a commit yet. `T011` should be committed once after final verification.

## Chunk 2: Usage Metrics, State, And Completion

### Task 2: Add failing tests for usage-diagnosis metrics and task-completion state

**Files:**
- Create: `tests/test_metrics_usage.py`
- Modify: `tests/test_persistence.py`
- Modify: `README.md`
- Modify: `continue/task.json`
- Modify: `continue/progress.md`
- Reference: `docs/superpowers/specs/2026-03-10-metrics-design.md`

- [ ] **Step 1: Write the failing test**

```python
from ciept.metrics.usage import image_shuffle_drop_rate, transported_mass_ratio


def test_usage_metrics_return_expected_relative_drops():
    ...
```

```python
def test_t011_marked_done_and_t012_selected_next():
    ...
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest tests/test_metrics_usage.py tests/test_persistence.py::test_t011_marked_done_and_t012_selected_next -v`
Expected: FAIL because usage metrics and the completed `T011` state do not exist yet.

- [ ] **Step 3: Write minimal implementation**

Create:

- `src/ciept/metrics/usage.py`

Update:

- `README.md` with metrics-layer notes
- `continue/task.json` to mark `T011` done and `T012` current
- `continue/progress.md` with a verification-backed `T011` entry

Implementation requirements:

- usage-diagnosis functions are relative and finite
- transported mass ratio stays bounded with epsilon protection
- README stays focused on formulas, not experiment orchestration

- [ ] **Step 4: Run test to verify it passes**

Run: `python -m pytest -v`
Expected: PASS with all repository tests green.

- [ ] **Step 5: Commit**

```bash
git add README.md continue/task.json continue/progress.md docs/superpowers/plans/2026-03-10-metrics.md src/ciept/metrics/__init__.py src/ciept/metrics/types.py src/ciept/metrics/ranking.py src/ciept/metrics/faithfulness.py src/ciept/metrics/usage.py tests/test_metrics_ranking.py tests/test_metrics_faithfulness.py tests/test_metrics_usage.py tests/test_persistence.py
git commit -m "feat: complete metrics task T011"
```
